{
  "name": "silieco",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:api": "npm run dev --workspace @silieco/api",
    "dev:web": "npm run dev --workspace @silieco/web",
    "dev:desktop": "npm run dev --workspace @silieco/desktop",
    "build": "npm run build --workspace @silieco/web",
    "start": "npm run start --workspace @silieco/api",
    "test": "npm run test --workspace @silieco/api && npm run test --workspace @silieco/runtime && npm run test --workspace @silieco/web",
    "check": "npm run check --workspace @silieco/api && npm run check --workspace @silieco/runtime && npm run check --workspace @silieco/desktop && npm run check --workspace @silieco/web"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
